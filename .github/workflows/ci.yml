name: CI

on:
  pull_request:
    branches: [ main ]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up environment
        run: |
          echo "Setting up environment"

      - name: Run tests
        run: |
          echo "Running tests"
          # npm test / pytest / mvn test etc.

      - name: Lint
        run: |
          echo "Running lint"
